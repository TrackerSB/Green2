language: java

install: true

env:
  - MODULE=EnvironmentHandler
  - MODULE=ConfigurationDialog
  - MODULE=Launcher
  - MODULE=Membermanagement

jdk:
  - oraclejdk8

sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer

notifications:
  email:
    recipients:
      - junktogo@web.de
    on_success: change
    on_failure: always
